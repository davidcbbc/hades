* Create a report that is deterministic and with screenshots - DONE
* Experiment the script on different websites
* Use login and non-login scripts
* Create different scripts with different strategies. One strategy is total anarchy so the LLM figures it out the path too the data breach posts and another strategy is to give the LLM the way to achieve that goal
* On the report create a table for the different forums and the metrics (login non-login, anarchy, stepped, etc...)
* Create a kibana dashboard or a simple flask application that lists the onions being monitored in yolo mode and deterministic and the results 
* Human on the loop for complex CAPTCHAS?